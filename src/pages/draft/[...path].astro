---
/**
 * FlashDraft - Dynamic Draft Routes
 * 
 * Handles:
 * - /draft/[draftId] -> Specific draft interface
 * - /draft/[draftId]/p[round]p[pick] -> Specific position in draft
 */

import Layout from '../../layouts/Layout.astro';
import { DraftInterface } from '../../components/DraftInterface';

export const prerender = false;
---

<Layout title="FlashDraft - Draft Interface">
  <DraftInterface client:load />
</Layout>

<style>
  /* Ensure full height for the app */
  html, body {
    height: 100%;
    margin: 0;
    padding: 0;
  }
</style>