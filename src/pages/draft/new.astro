---
/**
 * FlashDraft - New Draft Page
 * 
 * Simple page to redirect users to create a new draft.
 */

import Layout from '../../layouts/Layout.astro';
---

<Layout title="FlashDraft - New Draft">
  <main class="min-h-screen px-6 py-20">
    <div class="max-w-4xl mx-auto">
      <!-- Header -->
      <div class="text-center mb-16">
        <h1 class="text-5xl font-bold text-white mb-6">Start New Draft</h1>
        <p class="text-xl text-slate-300 max-w-2xl mx-auto">
          Choose from available MTG sets and begin your draft journey with realistic AI opponents
        </p>
      </div>
      
      <!-- Set Selection -->
      <div class="grid md:grid-cols-2 gap-8 max-w-4xl mx-auto">
        <!-- Final Fantasy Set -->
        <div class="bg-gradient-to-br from-slate-800/80 to-slate-900/80 backdrop-blur-sm rounded-3xl p-8 border border-slate-700/50 hover:border-slate-600/50 transition-all duration-300 group">
          <div class="text-center">
            <!-- Set Icon/Image placeholder -->
            <div class="bg-gradient-to-br from-purple-500 to-pink-500 w-24 h-24 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-200">
              <svg class="w-12 h-12 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"></path>
              </svg>
            </div>
            
            <h3 class="text-2xl font-bold text-white mb-3">Final Fantasy</h3>
            <div class="bg-purple-500/20 text-purple-300 px-3 py-1 rounded-full text-sm font-semibold inline-block mb-4 border border-purple-500/30">
              FIN
            </div>
            <p class="text-slate-300 mb-6 leading-relaxed">
              Magic: The Gathering × Final Fantasy crossover set featuring iconic characters, summons, and mechanics from the beloved RPG series.
            </p>
            
            <!-- Set Stats -->
            <div class="grid grid-cols-2 gap-4 mb-6 text-sm">
              <div class="bg-slate-700/30 rounded-xl p-3">
                <div class="text-slate-400">Cards</div>
                <div class="text-white font-semibold">~200</div>
              </div>
              <div class="bg-slate-700/30 rounded-xl p-3">
                <div class="text-slate-400">Type</div>
                <div class="text-white font-semibold">Modern</div>
              </div>
            </div>
            
            <button 
              onclick="startDraft('FIN')"
              class="w-full bg-gradient-to-r from-purple-600 to-pink-600 hover:from-purple-500 hover:to-pink-500 text-white px-6 py-4 rounded-xl font-semibold text-lg transition-all duration-200 shadow-lg hover:shadow-xl hover:scale-105"
            >
              Start FIN Draft
            </button>
          </div>
        </div>
        
        <!-- Tomorrow's Destiny Set -->
        <div class="bg-gradient-to-br from-slate-800/80 to-slate-900/80 backdrop-blur-sm rounded-3xl p-8 border border-slate-700/50 hover:border-slate-600/50 transition-all duration-300 group">
          <div class="text-center">
            <!-- Set Icon/Image placeholder -->
            <div class="bg-gradient-to-br from-orange-500 to-red-500 w-24 h-24 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-200">
              <svg class="w-12 h-12 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 18.657A8 8 0 016.343 7.343S7 9 9 10c0-2 .5-5 2.986-7C14 5 16.09 5.777 17.656 7.343A7.975 7.975 0 0120 13a7.975 7.975 0 01-2.343 5.657z"></path>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.879 16.121A3 3 0 1012.015 11L11 14H9c0 .768.293 1.536.879 2.121z"></path>
              </svg>
            </div>
            
            <h3 class="text-2xl font-bold text-white mb-3">Tomorrow's Destiny</h3>
            <div class="bg-orange-500/20 text-orange-300 px-3 py-1 rounded-full text-sm font-semibold inline-block mb-4 border border-orange-500/30">
              TDM
            </div>
            <p class="text-slate-300 mb-6 leading-relaxed">
              The future of Aetheros hangs in the balance as planeswalkers battle across time. Features powerful saga mechanics and temporal synergies.
            </p>
            
            <!-- Set Stats -->
            <div class="grid grid-cols-2 gap-4 mb-6 text-sm">
              <div class="bg-slate-700/30 rounded-xl p-3">
                <div class="text-slate-400">Cards</div>
                <div class="text-white font-semibold">~270</div>
              </div>
              <div class="bg-slate-700/30 rounded-xl p-3">
                <div class="text-slate-400">Type</div>
                <div class="text-white font-semibold">Modern</div>
              </div>
            </div>
            
            <button 
              onclick="startDraft('TDM')"
              class="w-full bg-gradient-to-r from-orange-600 to-red-600 hover:from-orange-500 hover:to-red-500 text-white px-6 py-4 rounded-xl font-semibold text-lg transition-all duration-200 shadow-lg hover:shadow-xl hover:scale-105"
            >
              Start TDM Draft
            </button>
          </div>
        </div>
      </div>
      
      <!-- Draft Info -->
      <div class="mt-16 text-center">
        <div class="bg-slate-800/50 backdrop-blur-sm rounded-2xl p-8 border border-slate-700/50 max-w-3xl mx-auto">
          <h3 class="text-xl font-bold text-white mb-4">What to Expect</h3>
          <div class="grid md:grid-cols-3 gap-6 text-sm text-slate-300">
            <div>
              <div class="font-semibold text-white mb-2">8 Players</div>
              <div>Draft against 7 AI opponents with realistic pick patterns</div>
            </div>
            <div>
              <div class="font-semibold text-white mb-2">3 Rounds</div>
              <div>15 picks per round, standard booster draft format</div>
            </div>
            <div>
              <div class="font-semibold text-white mb-2">~15 Minutes</div>
              <div>Complete a full draft session at your own pace</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>

  <script>
    async function startDraft(setCode) {
      try {
        // Generate a unique seed
        const seed = Date.now().toString();
        
        // Redirect to the create endpoint with the set and seed
        window.location.href = `/draft/create?set=${setCode}&seed=${seed}`;
      } catch (error) {
        console.error('Failed to start draft:', error);
        alert('Failed to start draft. Please try again.');
      }
    }
    
    // Make function globally accessible for inline onclick handlers
    window.startDraft = startDraft;
  </script>
</Layout>

<style>
  /* Ensure full height for the app */
  html, body {
    height: 100%;
    margin: 0;
    padding: 0;
  }
</style>
